# Nixpacks configuration - disable uv, use pip directly
[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

[variables]
NIXPACKS_NO_INSTALL_UV = "1"

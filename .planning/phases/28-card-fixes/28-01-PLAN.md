# Phase 28: Canvas Card Fixes — Execution Plan

## Objective

Fix 3 confirmed bugs blocking card editing and persistence, plus redesign cards to match Heptabase/Poppy.ai aesthetic. All code changes are already written — this plan covers verification and deployment.

## Status of Code Changes

### Already Implemented (code written, build passes):

1. **Editor crash fix** — `patched-suggestion.js` (NEW file)
   - Wraps `@tiptap/suggestion@3.19.0` to fix `this.key.getState is not a function`
   - `slash-extension.js` and `card-mention.js` updated to import patched version

2. **markdown-bridge.js** — Optional chaining fix
   - Line 15: `editor.storage.markdown?.getMarkdown?.() ?? editor.getText()`

3. **useAutoSave.js** — Flush on unmount
   - Lines 56-64: Fires pending save synchronously before cleanup

4. **useCardActions.js** — Full field sync
   - Line 25: `{ ...n.data, ...updated }` spreads all server-returned fields

5. **useDebouncedPositions.js** — Retry on failure
   - `flushPositions` retries once after 2s on save failure

6. **CanvasCard.css** — Complete Heptabase/Poppy.ai redesign
   - Top accent bar (2.5px), clean white surface, subtle type pill
   - Soft shadows with hover lift, 12px border-radius, no heavy dividers

7. **CanvasCard.jsx** — Removed inline `headerTint` style

## Execution Tasks

### Task 1: Verify Frontend Build
```bash
cd frontend && npm run build
```
**Success:** Zero errors, zero warnings related to canvas module.

### Task 2: Run Database Migration
```bash
cd /Users/sezars/llm-council && alembic upgrade head
```
**Why:** The `boards`, `cards`, and `edges` tables need migration `20260205_0003_add_boards_cards_edges.py`. Position persistence fails with 500 errors if tables don't have correct schema.
**Success:** Migration applies cleanly or is already at head.

### Task 3: Verify Editor Opens Without Crash
1. Start backend: `python -m backend.main`
2. Start frontend: `cd frontend && npm run dev`
3. Navigate to any board with cards
4. Click a note card to enter edit mode
5. **Success:** Editor opens, no console errors, no `getState is not a function`

### Task 4: Verify Card Position Persistence
1. Drag 2-3 cards to new positions
2. Wait 1 second (debounce fires at 500ms)
3. Refresh the page (Cmd+R)
4. **Success:** Cards remain in their new positions after refresh

### Task 5: Verify Autosave
1. Click a note card to edit
2. Type some content
3. Watch for "Saving..." → "Saved" status indicator
4. Click outside the card to close editor
5. Refresh the page
6. **Success:** Content persists after refresh

### Task 6: Verify Card Visual Design
1. Check card appearance matches Heptabase/Poppy.ai style:
   - Top accent bar (thin colored line at top)
   - Clean white background, no heavy borders
   - Type label as subtle pill with accent tint
   - Soft shadow with hover lift effect
   - 12px rounded corners
2. Check all card types render correctly (note, knowledge, query, response, synthesis, file, link)
3. Toggle dark mode — verify cards look good
4. **Success:** Cards match Heptabase aesthetic, all types render, dark mode works

### Task 7: Verify Card Resize Persistence
1. Drag the resize handle on a card to change width
2. Release mouse
3. Refresh the page
4. **Success:** Card width persists after refresh

## Verification Checklist

- [ ] `npm run build` — zero errors
- [ ] `alembic upgrade head` — migration applied
- [ ] Editor opens without crash
- [ ] Position saves persist after refresh
- [ ] Autosave works (typing → saving → saved → persists)
- [ ] Card design matches Heptabase/Poppy.ai
- [ ] Card resize persists
- [ ] Zero console errors during all tests
- [ ] Dark mode cards render correctly

## Files Changed (Summary)

| File | Change | Bug Fixed |
|------|--------|-----------|
| `editor/patched-suggestion.js` | NEW | `getState is not a function` crash |
| `editor/slash-extension.js` | Import change | Uses patched suggestion |
| `editor/card-mention.js` | Import change | Uses patched suggestion |
| `editor/markdown-bridge.js` | Optional chaining | Crash on missing markdown storage |
| `editor/useAutoSave.js` | Flush on unmount | Data loss on unmount |
| `hooks/useCardActions.js` | Spread all fields | Incomplete state sync |
| `hooks/useDebouncedPositions.js` | Retry mechanism | Position save failure |
| `styles/CanvasCard.css` | Full redesign | Visual style mismatch |
| `components/CanvasCard.jsx` | Remove inline style | Style conflict |

## Rollback

All changes are on the `feature/deploy-vercel-railway` branch. If anything breaks:
```bash
git checkout -- frontend/src/modules/canvas/
```
